# 작업 관리 대장 (백로그)

작성일: 2026-07-21
성격: **살아있는 문서**. 할 일의 단일 소스(single source of truth). [02_WBS](../02_WBS_작업분해/WBS_작업분해.md)가 "범위를 한 번 쪼갠 스냅샷"이라면, 이 문서는 이후 실제로 진행되는 작업을 상태와 함께 추적한다.
갱신 규칙: 상태가 바뀌면 표에서 바로 수정. 새 할 일은 아래 규칙대로 ID를 부여해 추가. 완료 항목은 "완료" 섹션으로 이동.

## 범례

- **우선순위**: `P0` 지금 막힌 것/필수, `P1` 이번 마일스톤, `P2` 이후
- **상태**: `Backlog` → `진행` → `리뷰` → `완료` (필요 시 `보류`)
- **버킷**: 블로커 / 기능격차 / 품질·기반 / 보안 / (결정대기는 별도 섹션)
- **담당**: 개발(개발자1·PM) / 대표·PM / — . 세부 R&R은 두지 않으므로 실제 잡는 사람이 이름을 적는다.

## 개발 단계 (2026-07-29 대표님 지시 반영)

대표님이 전달한 우선순위 피드백(P0/P1/P2, 11개 항목)을 기존 백로그와 통합해 **5단계**로 재편했다. 위에서 아래 순서로 진행한다.

| 단계 | 이름 | 목표 | 근거 |
|---|---|---|---|
| **S0** | 빌드 기반 선행 | 클린 체크아웃에서 빌드·배포가 재현됨 | 스모크 테스트 실측(대표님 목록에 없던 항목) |
| **S1** | 계약 확정 (대표 P0) | 토큰 계약·파서 연결고리·sanitizer — **법제처 연동의 설계 전제조건** | 대표님 P0 |
| **S2** | 법제처 API 연동 (대표 P1) | 외부 법령 데이터를 조·항·목으로 수용 | 대표님 P1 |
| **S3** | 연동 마무리·안정화 (대표 P2) | 보안/안정성 부채 정리 | 대표님 P2 |
| **S4** | 잔여 기능격차 | SRS 13절 미구현 항목 정리 | 기존 백로그 |

> **순서 근거(대표님 문서)**: S1을 먼저 하는 이유는 토큰 계약과 파서-구조 연동이 법제처 연동의 전제조건이기 때문. 계약 없이 외부 데이터를 붙이면 재작업이 발생한다. S2는 붙는 순간 S1-2의 자동매핑 문제를 상당 부분 흡수하는 시너지가 있어 직후 착수가 효율적.

### ⚠️ 착수 전 정정사항 (코드 실측 결과)

대표님 문서는 v1~v3·개발정의서 기반 분석이라 실제 코드와 3가지 차이가 있다. 작업 범위에 반영했다.

| 문서 서술 | 코드 실측 | 영향 |
|---|---|---|
| 지원 토큰에 `{{article.number}}` 포함 | **미지원**. 최상위 토큰은 `tenant.name`/`policy.title`/`policy.code`/`today`/`revisionDate`/`effectiveDate`/`content`/`logo`뿐. 조문은 `{{content}}` 안에 렌더됨 | S1-1에서 실제 토큰만 명세 |
| `docs/template-feature-test-checklist.md`에 수동 QA 체크리스트 존재 | **파일 없음** | S3-10 E2E 작업 시 체크리스트도 신규 작성 필요 |
| 마이그레이션 "총 6건 이상 누적" | 실제 **12건**, 단 baseline 부재로 `migrate deploy` 실패 → `db push` 폴백 | S0-3 유지 |

**추가 발견(실제 버그)**: 설정 화면 템플릿 미리보기가 `revisionDate`·`effectiveDate`를 데이터로 넘기지 않아, 해당 토큰을 쓴 템플릿이 **미리보기에서는 빈칸, 실제 규정에서는 정상** 렌더된다. S1-1에서 함께 수정.

## 마일스톤 (묶어서 낼 단위)

| 마일스톤 | 목표 | 포함 단계 | 상태 |
|---|---|---|---|
| M1. 빌드 복구 | 클린 체크아웃에서 `docker compose up`이 그대로 성공 | S0 | **완료(2026-08-02)** |
| M2. 계약 확정 | 토큰·파서·sanitizer 계약 확립 | S1 | **완료(2026-07-30)** |
| M3. 법제처 연동 | 외부 법령 가져오기 동작 | S2 | 진행 (백엔드 완료 · 화면 T-55 잔여) |
| M3'. 자체 데이터로 패리티 | 법제처 없이도 규정 유입·비교가 성립 | T-12 · T-56 · T-71 | 진행 (T-12·T-56 대비표 완료, T-71 잔여) |
| M4. 출시 준비 | 보안·안정화·잔여 기능 | S3 + S4 | Backlog |

## 칸반 뷰 (요약)

- **진행**: — (S0·S1 완료 / 8-12~13 T-12·T-40·T-31·T-87·T-86 완료, T-30 부분)
- **리뷰**: —
- **다음(순서대로)**: T-56 3단비교 → T-30 잔여 → T-41 격리감사 → S3
- **Backlog(S4 잔여)**: T-10·T-11·T-13·T-15~T-17·T-88·T-89, T-30(잔여)·T-32, T-41·T-42
- **Backlog(S5 레퍼런스 패리티, 대표 지시 8/1)**: T-71·T-73~T-75·T-77~T-81·T-85 (T-70·T-72·T-76(PDF) 완료)
- **완료**: C-01 ~ C-20 (아래 완료 섹션)

---

## S0. 빌드 기반 선행 (M1) — 이거부터

| ID | 제목 | 우선순위 | 담당 | 완료 정의(DoD) | 출처 | 상태 |
|----|------|:---:|---|---|---|:---:|
| T-01 | Docker 빌드 복구 | P0 | 개발 | Debian slim 전환 + `--only-binary=:all:`. 원본 Dockerfile로 빌드·기동, 런타임 `import pdfplumber, fitz` 확인 | [기술부채 #7](../13_기술부채대장/기술부채_대장.md) · [ADR-0013](../10_ADR_의사결정기록/ADR.md) | **완료(8/2)** |
| T-02 | 비결정적 빌드 제거 | P0 | 개발 | 빌드 컨텍스트를 리포 루트로 올려 워크스페이스 락파일 사용, `npm ci --workspace=apps/api`로 전환 | [기술부채 #6](../13_기술부채대장/기술부채_대장.md) | **완료(8/2)** |
| T-03 | Prisma baseline 마이그레이션 생성 | P0 | 개발 | 13건 → baseline 2건으로 스쿼시. 빈 DB에서 `migrate deploy`만으로 재현(드리프트 0), `db push --accept-data-loss` 폴백 제거, baseline 이전 DB 자동 처리 | [기술부채 #8](../13_기술부채대장/기술부채_대장.md) · [ADR-0012](../10_ADR_의사결정기록/ADR.md) | **완료(8/2)** |

## S1. 계약 확정 — 대표 P0 (M2)

법제처 연동의 **설계 전제조건**. 여기서 계약을 못 박지 않으면 S2에서 재작업이 발생한다.

| ID | 제목 | 우선순위 | 담당 | 완료 정의(DoD) | 출처 | 상태 |
|----|------|:---:|---|---|---|:---:|
| T-50 | 가변 변수(토큰) 스키마 확정·단일소스화 | P0 | 개발 | ① 레지스트리 단일 파일 ② 렌더·미리보기 같은 빌더 ③ 미리보기 누락 버그 수정 ④ 문서화 | 대표 P0-1 | **완료(7/30)** |
| T-51 | Import 파서 → `clauseNumber`/`itemNumber` 자동 추론 | P0 | 개발 | ①②… / 1. 2. … 인식해 항·목 등록. 수동 구조편집 불필요 | 대표 P0-2 · [SRS 13.4](../03_SRS_요구사항명세/SRS_요구사항명세.md) | **완료(7/30)** |
| T-52 | HTML/CSS sanitizer 정규식 → 라이브러리 전환 | P0 | 개발 | 서버 sanitize-html · 클라이언트 DOMPurify 교체. XSS 벡터 11건 차단 확인 | 대표 P0-3 · v2 미해결 과제 | **완료(7/30)** |

## S2. 법제처 API 연동 — 대표 P1 (M3)

> **2026-08-13 방침 변경(보류 해제)**: 법제처 OPEN API 사용 승인이 나면서 T-53·T-54를 착수·완료했다.
> 남은 것은 화면(T-55)뿐이다. 아래 8/12 방침은 이력으로 남긴다.
>
> <details><summary>2026-08-12 방침 (보류 당시)</summary>
>
> 법제처 API 연동을 당장 하지 않기로 하고, 연동 없이 가능한 작업을 먼저 진행한다.
> T-53~T-55는 보류. **T-56은 보류 대상이 아니다** — 신구조문대비표는 우리 규정끼리 비교하는 기능이라
> 외부 데이터가 필요 없고, T-72(시점 조회) 인프라 위에서 바로 만들 수 있다.
> 연동을 안 해도 기존 기능에 회귀는 없다: 판·법·규 관련정보 패널은 `RELATED_PREVIEW_API_URL`이
> 설정된 적이 없어 지금도 테넌트 내부 데이터로만 동작한다.
> </details>

**운영 전제 (놓치면 운영에서만 깨진다)**: 법제처는 인증값(`LAW_GO_KR_OC`)과 **호출 서버의 공인 IP**를
함께 검증한다. 배포 서버 IP를 open.law.go.kr > 마이페이지 > API인증키관리에 등록하지 않으면
로컬에서 되던 호출이 운영에서 `사용자 정보 검증에 실패하였습니다`로 떨어진다. 이 응답은 **HTTP 200**으로
오기 때문에 상태 코드만 보는 코드는 오류를 성공으로 오인한다 (`lawgokr.service.ts`가 본문 모양으로 판별).

| ID | 제목 | 우선순위 | 담당 | 완료 정의(DoD) | 출처 | 상태 |
|----|------|:---:|---|---|---|:---:|
| T-53 | 백엔드 프록시 모듈 신설 (`apps/api/src/lawgokr/*`) | P1 | 개발 | 법제처 Open API 키를 서버에만 두고 검색(getLsSearch)·본문(getLsInfo) 래핑, Redis 응답 캐싱. 프론트 직접 호출 금지(키 노출·CORS 방지) | 대표 P1-4 | **완료(8/13)** |
| T-54 | 법제처 응답 → 조·항·목 구조 매퍼 | P1 | 개발 | 법제처 XML/JSON(조/항/호)을 내부 Article 계층으로 변환. T-51 파서와 인터페이스 공유 | 대표 P1-5 | **완료(8/13)** |
| T-55 | Import Wizard에 "법제처에서 가져오기" 탭 | P1 | 개발 | 법령명·조문 검색 → 미리보기 → 등록. 기존 파일/텍스트 업로드와 동일 패턴 재사용. **백엔드는 준비됨**: `GET /lawgokr/search`로 목록, `GET /lawgokr/laws/:mst`가 PDF 경로와 같은 `tree.roots`를 주므로 `PolicyImportPage`의 미리보기·편집·커밋 단계를 그대로 재사용한다. 남은 일은 파싱 세션 생성 경로(현재 `RegulationParseSession`은 업로드 파일이 있어야 만들어진다)와 탭 UI | 대표 P1-6 | **완료(8/17)** |
| T-56 | 신구조문대비표 / 3단비교 | P1 | 개발 | **신구조문대비표 완료(8/13)** — `GET :id/compare?from&to`가 두 시점 as-of 스냅샷을 `articleId` 기준으로 짝지어 신설/삭제/개정/동일 판정 + 낱말 diff. 2단 대비표 화면·인쇄 제공. **3단비교는 T-71(규정 간 관계) 선행 필요 → 미착수** | 대표 P1-7 · [화면설계서 13.3](../11_화면설계서/화면설계서.md) | **대비표 완료(8/13)** |

## S3. 연동 마무리·안정화 — 대표 P2 (M4)

| ID | 제목 | 우선순위 | 담당 | 완료 정의(DoD) | 출처 | 상태 |
|----|------|:---:|---|---|---|:---:|
| T-57 | 테넌트 브랜딩 서버 동기화 | P2 | 개발 | localStorage 전용 → 서버 저장. 기기·브라우저 변경 시 유실 없음(Pro+ 유료 기능 리스크 해소) | 대표 P2-8 · [ADR-0003](../10_ADR_의사결정기록/ADR.md) | Backlog |
| T-58 | 복원(restore) 전용 API 엔드포인트 분리 | P2 | 개발 | 감사로그에서 `template.update`와 `template.restore`가 구분됨 | 대표 P2-9 | Backlog |
| T-59 | 플랜×권한 조합 E2E 테스트 자동화 | P2 | 개발 | Starter/Pro/Enterprise × 템플릿 API 403/기능노출 자동 검증. **수동 체크리스트 문서도 신규 작성**(현재 부재) | 대표 P2-10 · [기술부채 #1](../13_기술부채대장/기술부채_대장.md) | Backlog |
| T-60 | 조 번호 일괄 재정렬(드래그 앤 드롭) | P2 | 개발 | 목차에서 조 순서 변경·번호 재부여. 기존 데이터 정리 반복 부담 해소 | 대표 P2-11 | Backlog |

## S4. 잔여 기능 격차 (M4)

미구현/placeholder를 "구현" 또는 "명시적 제외"로 정리한다. (대표님 목록에 없으나 SRS에 남은 항목)

| ID | 제목 | 우선순위 | 담당 | 완료 정의(DoD) | 출처 | 상태 |
|----|------|:---:|---|---|---|:---:|
| T-86 | ~~C-12 DB 실측~~ | P1 | 개발 | 빈 볼륨에서 `migrate deploy` 3건 적용·인덱스 생성 확인. as-of를 제정 전/후·개정 후 3개 시점으로 검증(0건→2건→4건), 경계·잘못된 날짜(400)·미인증(401)·**교차 테넌트 404** 확인. 개정이유 CRUD·PDF 내보내기·PDF 가져오기 전 구간 실측 | C-12 잔여 | **완료(8/13)** |
| T-87 | ~~절 사용 시 조 번호 역전~~ | P1 | 개발 | 절 미소속 조를 앞에 몰아 제4조가 제1절 제3조보다 먼저 출력되던 문제. `blocks`(조 번호순)·`allGroups` 도입. **부수 발견: txt 내보내기·검색 매치 카운트가 절 소속 조문을 통째로 누락하던 것도 함께 수정** | 8/7 발견 | **완료(8/12)** |
| T-10 | 버전 반려(reject)·폐지(archive) UI 연결 | P1 | 개발 | 규정 상세에서 reject/archive 동작 노출·호출, 상태 전이 확인 | [SRS 13.2](../03_SRS_요구사항명세/SRS_요구사항명세.md) | Backlog |
| T-11 | 감사 로그 조회 화면 | P1 | 개발 | `GET /audit-logs`를 admin 화면에서 조회·필터 | [SRS 13.2](../03_SRS_요구사항명세/SRS_요구사항명세.md) | Backlog |
| T-12 | 규정 가져오기(PDF, regulation-parse) UI | P1 | 개발 | 업로드→트리 미리보기·수정→커밋. `/policies/import` 신규 화면 | [SRS 13.2](../03_SRS_요구사항명세/SRS_요구사항명세.md) | **완료(8/12)** |
| T-88 | PDF 가져오기 커밋 시 항·목 계층 반영 | P1 | 개발 | `regulation-parse` 커밋이 트리를 전부 **조(條)로 평탄화**한다. ①② 자식이 제3조의 항이 아니라 제4·5조가 된다. 텍스트 경로(T-51 `policyImportHierarchy`)와 달리 `clauseNumber`/`itemNumber`를 쓰지 않음 | T-12 실측(8/13) | **완료(8/17)** |
| T-89 | PDF 가져오기: 장(章) 구조·조 제목 정리 | P2 | 개발 | ① 장 제목이 인식되지 않아 항상 `본문`(suppressHeader) 1개 장에 담김 ② 다음 장 제목이 직전 조 본문에 섞여 들어감 ③ 조 제목이 `[L1] 목적` 형태로 내부 norm이 노출됨 | T-12 실측(8/13) | **완료(8/18)** |
| T-13 | 관리자 벌크 임포트 UI | P2 | 개발 | `POST /admin/import/policies`를 화면에서 실행 | [SRS 13.2](../03_SRS_요구사항명세/SRS_요구사항명세.md) | Backlog |
| T-14 | ~~가져오기 시 항·목 자동 매핑~~ | — | — | **T-51로 승격·통합**(대표 P0-2) | — | 통합됨 |
| T-15 | 규정 `isActive` 토글 편집 UI | P2 | 개발 | 목록/상세에서 활성·비활성 전환 가능 | [SRS 13.3](../03_SRS_요구사항명세/SRS_요구사항명세.md) | Backlog |
| T-16 | Export(내보내기): 구현 or 스키마 제거 결정·반영 | P2 | 개발 | ExportModule 실제 구현 **또는** ExportJob 스키마·잔재 정리 | [SRS 13.1](../03_SRS_요구사항명세/SRS_요구사항명세.md) | Backlog |
| T-17 | Billing 결제 이력·구독 기록 구현 or 정리 | P2 | 개발 | 결제 시 BillingPayment/Subscription 기록 **또는** 미사용 테이블 정리 방침 확정 | [SRS 13.1](../03_SRS_요구사항명세/SRS_요구사항명세.md) | Backlog |
| T-18 | ~~규정 상세 disabled 확장버튼 8종 처리 방침~~ | — | — | **S5로 대체.** 2026-08-01 대표 지시로 "전부 구현"으로 방침 확정(숨김·제외 선택지 소멸). 8종 중 별표·서식은 이미 보유(C-08에서 도구줄 오안내 수정) | [화면설계서 13.3](../11_화면설계서/화면설계서.md) | 결정됨 |
| T-19 | ~~테넌트 브랜딩 서버 동기화~~ | — | — | **T-57로 승격·통합**(대표 P2-8) | — | 통합됨 |

## S6. 규정 구조 유연화 — 대표 지시 (2026-08-02)

**지시 원문 취지**: ① 장 없이 조부터 추가 가능해야 한다 ② 결재란은 없어도 된다 ③ 조·항·목 표기를 법령처럼(제N조/①②/1.) ④ 기준일을 개정일·시행일로 구분 ⑤ 개정 알림 검토. 추가로 절(節) 계층을 **null 허용**으로 신설하기로 확정.

### 착수 전 코드 실측 — 5건 중 3건은 이미 구현 상태였음

| 대표 요구 | 실측 결과 | 조치 |
|---|---|---|
| 조·항·목 표기(제N조/①②/1.) | ✅ **이미 구현** — `legalArticleLabel.ts`가 정확히 그 형식 | 문서에 요구사항으로 명문화(SRS POLICY-10) |
| 기준일 → 개정일/시행일 구분 | ✅ **이미 구현** — `revisionDate`·`effectiveDate` 분리, "기준일" 문구 없음 | SRS POLICY-11로 명문화 |
| 개정 알림 | ✅ **이미 구현** — 알림 수신 팀·개인 지정, 시행 승인 시 자동 발송 | — |
| 결재란(작성·검토·승인) | ✅ **이미 선택 항목** — `showApprovalLine` 기본 `false` | SRS TPL-7로 명문화. **워크플로(초안→검토→승인)가 아니라 인쇄 문서의 서명칸을 뜻함**을 대표 확인(2026-08-02) |
| 장 없이 조부터 | ⚠️ **부분** — 상세 화면은 이미 숨김 장 자동 생성. 단 **가져오기에서 "제1장 총칙"이 강제 생성**됨 | T-82에서 수정 |

| ID | 제목 | 우선순위 | 담당 | 완료 정의(DoD) | 출처 | 상태 |
|----|------|:---:|---|---|---|:---:|
| T-82 | 절(節) 계층 신설 (선택 계층) | P0 | 개발 | Section 모델 + `Article.sectionId` nullable. 절 CRUD API, 목차·전문보기·인쇄 반영. 절 미소속 조와 공존 | 대표 지시 8/2 · [ADR-0011](../10_ADR_의사결정기록/ADR.md) | **완료(8/2)** |
| T-83 | 가져오기 시 가짜 "제1장 총칙" 제거 | P0 | 개발 | 원문에 장이 없으면 숨김 장에 담아 등록(임의 장 제목 생성 금지) | 대표 지시 8/2 · SRS POLICY-6 | **완료(8/2)** |
| T-84 | `Article.chapterId` nullable 전환 검토 | P2 | 개발 | 조가 장 없이도 존재하는 정석 구조로 이행할지 결정. 테스트 기반(T-30) 확보 후 재검토 | [ADR-0011](../10_ADR_의사결정기록/ADR.md) 대안 | Backlog |

## S5. 레퍼런스 사이트 기능 패리티 — 대표 지시 (2026-08-01)

**지시 원문 취지**: 레퍼런스 사이트 2곳이 법령을 조회할 때 지원하는 기능은 VEDA도 **무조건** 보유해야 한다.

- 레퍼런스 A: [국가법령정보센터](https://www.law.go.kr/main.html) — 법령 본문 열람의 사실상 표준
- 레퍼런스 B: [KRX 법무포털](https://rule.krx.co.kr/out/index.do) — 자율규정(사규 성격) 열람의 국내 사례

이 지시로 T-18(“8종 버튼을 구현할지 숨길지”)은 **구현으로 확정**되었다. 아래는 두 사이트의 조회 기능을 항목화해 VEDA 현황과 대조한 것이다.

### 대조표 (✅ 보유 / ⚠️ 부분 / ❌ 미보유)

| # | 레퍼런스 기능 | 출처 | VEDA 현황 | 대응 |
|---|---|---|---|---|
| 1 | 조·항·목 계층 본문 열람 | A·B | ✅ | — |
| 2 | 조문 선택 이동(조문 점프) | A | ✅ | — |
| 3 | 본문 내 검색 + 이전/다음/첫 일치 이동 | A | ✅ | — |
| 4 | 전문 보기·인쇄(A4·페이지번호) | A·B | ✅ | — |
| 5 | 부칙·별표·서식 | A·B | ✅ 사이드바 탭 | C-08에서 도구줄 오안내 수정 |
| 6 | 통합검색 | A·B | ✅ | — |
| 7 | 개정 알림·구독(메일링) | A | ✅ 알림그룹·개정통지 | — |
| 8 | 외부 판례·법령 검색 연결(판·법·규) | A | ✅ | — |
| 9 | 조문 단위 버전 비교 | A(신구법비교의 일부) | ✅ 조문 diff + **문서 전체 대비표(8/13)** | T-56(대비표) 완료 |
| 10 | 조문 단위 안정 링크(제N조 기준 딥링크) | A | ⚠️ UUID 해시(`#article-<uuid>`)뿐 | T-73 |
| 11 | 제정·개정이유(개정문) | A | ✅ **구현(8/7)** | T-70 완료 |
| 12 | 신구조문대비표(문서 전체 대비) | A·B | ✅ **구현(8/13)** 두 시점 조문 단위 대비 | T-56(대비표) 완료 |
| 13 | 3단비교(규정–시행세칙–지침) | A | ❌ | T-56 |
| 14 | 규정 체계도(상·하위 관계도) | A(법령체계도) | ❌ | T-71 |
| 15 | 시점 조회(특정 시행일 기준 본문) | A·B | ✅ **구현(8/7)** 기준일 기준 전문 재구성 | T-72 완료 |
| 16 | 선택 조문만 인쇄 | A | ❌ | T-74 |
| 17 | 화면 확대/축소 | A | ❌ | T-75 |
| 18 | HWP·PDF 저장 | B(한글저장) | ⚠️ **PDF 구현(8/7)**, HWP 미착수 | T-76(PDF) 완료 · HWP는 T-85 |
| 19 | 사전식(가나다) 검색 | B | ❌ | T-77 |
| 20 | 최근 개정 규정 목록 · 제·개정 예고 | B | ❌ | T-78 |
| 21 | 검색 자동완성·검색기록 | A | ❌ | T-79 |
| 22 | 즐겨찾기·마이페이지 저장 | A | ❌ | T-80 |
| 23 | 음성지원·점자뷰어(접근성) | A | ❌ | T-81 |

### 작업

| ID | 제목 | 우선순위 | 담당 | 완료 정의(DoD) | 출처 | 상태 |
|----|------|:---:|---|---|---|:---:|
| T-70 | 제정·개정이유(개정문) 보관·열람 | P1 | 개발 | `PolicyRevisionReason` 모델 + CRUD. 문서 차수별 이유와 **조문별 개정 사유(`changeNote`)를 한 화면에서 대조** | 대표 지시 8/1 · 대조표 #11 | **완료(8/7)** |
| T-71 | 규정 체계도(상·하위 관계) | P2 | 개발 | 규정 간 상위/하위(규정–세칙–지침) 관계 등록 + 체계도 시각화. **T-56의 3단비교와 데이터 모델 공유** | 대표 지시 8/1 · 대조표 #14 | **완료(8/19)** |
| T-72 | 시점 조회(특정 시행일 기준 본문) | P1 | 개발 | `ArticleVersion.effectiveDate` 신설 + 승인 시 확정. `GET :id/as-of`가 전문 보기와 같은 형태로 응답해 인쇄·PDF까지 재사용 | 대표 지시 8/1 · 대조표 #15 | **완료(8/7)** |
| T-73 | 조문 안정 링크(제N조 기준 딥링크) | P2 | 개발 | `#제3조`·`?jo=3&hang=1` 형태의 UUID 비의존 링크. 주소 복사가 현재 조문을 가리킴 | 대표 지시 8/1 · 대조표 #10 | Backlog |
| T-74 | 선택 조문만 인쇄 | P2 | 개발 | 목차에서 조문 체크 → 선택분만 전문 보기·인쇄 | 대표 지시 8/1 · 대조표 #16 | Backlog |
| T-75 | 본문 화면 확대/축소 | P2 | 개발 | 본문 글자크기 단계 조절 + 설정 유지 | 대표 지시 8/1 · 대조표 #17 | Backlog |
| T-76 | HWP·PDF 내보내기 | P1 | 개발 | **PDF 완료** — PyMuPDF Story로 서버 렌더, 전문 보기 HTML을 그대로 변환. **HWP는 미착수(T-85로 분리)** | 대표 지시 8/1 · 대조표 #18 · T-16과 통합 검토 | **PDF 완료(8/7)** |
| T-85 | HWP 내보내기 | P2 | 개발 | PDF(T-76)와 별개. HWP는 바이너리 독점 포맷이라 변환 수단(라이브러리/외부 변환기) 선정이 선행 과제 | T-76에서 분리 | Backlog |
| T-77 | 사전식(가나다) 규정 색인 | P2 | 개발 | 규정명 초성·가나다 색인 브라우징 | 대표 지시 8/1 · 대조표 #19 | Backlog |
| T-78 | 최근 개정 목록 · 제·개정 예고 | P2 | 개발 | 대시보드/목록에 최근 개정 N건, 예고(시행 예정) 규정 노출 | 대표 지시 8/1 · 대조표 #20 | Backlog |
| T-79 | 검색 자동완성·검색기록 | P2 | 개발 | 통합검색 입력 시 규정명·조 제목 자동완성, 최근 검색어 저장·삭제 | 대표 지시 8/1 · 대조표 #21 | Backlog |
| T-80 | 즐겨찾기(규정·조문 저장) | P2 | 개발 | 사용자별 규정/조문 즐겨찾기 + 마이페이지 목록 | 대표 지시 8/1 · 대조표 #22 | Backlog |
| T-81 | 접근성(음성지원·점자뷰어) | P2 | 개발 | 본문 TTS 재생, 점자뷰어 호환 마크업. 범위·수단 사전 검토 필요 | 대표 지시 8/1 · 대조표 #23 | Backlog |

> **주의**: 두 레퍼런스는 *공표된 법령·자율규정*을 다루는 공개 사이트이고 VEDA는 *테넌트 사내 규정*을 다룬다. 위 기능은 그대로 옮기되 **멀티테넌시 격리**(T-41)를 깨지 않는 범위에서 구현한다. 특히 T-73(안정 링크)·T-80(즐겨찾기)은 테넌트 경계 검증을 DoD에 포함할 것.

## 3. 품질·기반 (M2)

| ID | 제목 | 우선순위 | 담당 | 완료 정의(DoD) | 출처 | 상태 |
|----|------|:---:|---|---|---|:---:|
| T-30 | 핵심 플로우 자동화 테스트 도입 | P1 | 개발 | **부분 완료(8/12)**: vitest 도입 + 순수 로직 28건(조·항·목 구조, 절 순서, 날짜, 시크릿 가드). **잔여 — DB 필요한 플로우(로그인·규정 CRUD·버전 승인)는 컨테이너 기동이 전제라 미착수** | [기술부채 #1](../13_기술부채대장/기술부채_대장.md) | **부분 완료** |
| T-31 | CI 파이프라인 구성 | P1 | 개발 | `.github/workflows/ci.yml` — push/PR마다 typecheck·test·build + **PDF 렌더러 파이썬 스모크**(빌드는 통과하는데 런타임에만 터지는 경로) | [기술부채 #2](../13_기술부채대장/기술부채_대장.md) | **완료(8/12)** |
| T-32 | eslint 설정 파일 정비 | P2 | 개발 | `npm run lint`가 실제 룰로 동작(설정 파일 존재) | [기술부채](../13_기술부채대장/기술부채_대장.md) · [개발컨벤션 4절](../05_개발컨벤션/개발컨벤션.md) | Backlog |

## 4. 보안 (M3)

| ID | 제목 | 우선순위 | 담당 | 완료 정의(DoD) | 출처 | 상태 |
|----|------|:---:|---|---|---|:---:|
| T-40 | docker-compose 기본 시크릿 운영 교체 절차 확정·문서화 | P0(운영 전) | 개발 | compose 시크릿을 `${VAR:-default}`로 전환해 override 가능하게 하고, `secrets-guard.ts`가 운영 기동 시 기본값을 감지하면 **부팅을 거부**. Runbook 7-1에 절차 | [보안검토서 1](../15_보안검토서/보안_검토서.md) · [기술부채 #4](../13_기술부채대장/기술부채_대장.md) | **완료(8/12)** |
| T-41 | 멀티테넌시 `tenantId` 격리 코드 감사 | P1 | 개발 | 전 서비스 쿼리에 tenant 필터 존재 확인, 누락 0건 | [보안검토서](../15_보안검토서/보안_검토서.md) · [ADR-0002](../10_ADR_의사결정기록/ADR.md) | Backlog |
| T-42 | Google OAuth state/콜백 검증 감사 | P2 | 개발 | state 위조·재사용 방어 검토 완료 | [보안검토서](../15_보안검토서/보안_검토서.md) | Backlog |

---

## 완료 (참고 기록)

| ID | 제목 | 완료일 | 비고 |
|----|------|---|---|
| C-01 | Deliverables 문서 체계 수립 | 2026-07-21 | 폴더 구조 + 16개 문서 초안 |
| C-02 | 루트 README 작성 | 2026-07-21 | [기술부채 #3 해결](../13_기술부채대장/기술부채_대장.md) |
| C-03 | Prisma 스키마 블록 주석 `/**` → `///` 수정 | 2026-07-21 | 스모크 테스트 중 발견·수정, [기술부채 #5 해결](../13_기술부채대장/기술부채_대장.md) |
| C-11 | S0 빌드 블로커 3건 해소 (T-01·T-02·T-03) | 2026-08-02 | Debian 전환·루트 컨텍스트+npm ci·baseline 스쿼시. **빈 볼륨에서 `down -v` → `up -d --build` 전체 재현 검증**(마이그레이션만으로 기동, 로그인·규정·절 API·PDF import 확인) |
| C-09 | T-82 절(節) 계층 신설 | 2026-08-02 | Section 모델·마이그레이션·CRUD API·목차/전문보기/인쇄 반영. API 실측: 절 소속/미소속 조 공존, 교차 장 참조 400 거부, 절 삭제 시 조문 보존 확인 |
| C-10 | T-83 가져오기 가짜 장 제거 | 2026-08-02 | 파서에 `auto` 플래그 추가해 원문에 장이 없을 때 숨김 장으로 등록 |
| C-04 | `docker compose up` 스모크 테스트 | 2026-07-21 | 코어 스택 검증 + 빌드 블로커 4건 식별, [Runbook 9절](../16_Runbook_배포롤백/Runbook_배포롤백.md) |
| C-05 | T-50 토큰 스키마 단일소스화 | 2026-07-30 | `templateTokens.ts` 신설(8종 레지스트리). 미리보기 `revisionDate`·`effectiveDate` 누락 버그 수정, 에디터에 토큰 목록·오타 경고 노출 |
| C-06 | T-51 Import 항·목 자동 추론 | 2026-07-30 | `policyImportHierarchy.ts` 신설. ①②→항, 1. 2.→목 인식. 날짜 오인·부모없는 목 방어. API 실측으로 계층 저장 확인 |
| C-07 | T-52 sanitizer 라이브러리 전환 | 2026-07-30 | 서버 `sanitize-html`, 클라이언트 `dompurify`. 기존 정규식은 무따옴표 `onerror=` 등 6벡터 중 5개 우회 → 교체 후 11벡터 전부 차단 |
| C-12 | S5 P1 3건 (T-70·T-72·T-76 PDF) | 2026-08-07 | **T-72 시점 조회**: `ArticleVersion.effectiveDate` 신설(마이그레이션에서 기존 시행분 `approved_at` 기준 backfill), 승인 시 시행일 확정, `GET :id/as-of`가 `findOne`과 같은 형태로 응답해 전문 보기·인쇄·PDF 재사용. **T-70 제·개정이유**: `PolicyRevisionReason` 모델 + CRUD + 모달(문서 차수 이유와 조문별 `changeNote` 대조). **T-76 PDF**: PyMuPDF Story 서버 렌더(`scripts/pdf_render.py`), wheel 동봉 CJK 폰트로 컨테이너 폰트 설치 불필요, `subset_fonts()`로 3.5MB→약 16KB. 검증: 실제 `buildEnterprisePolicyBodyHtml` 출력 → PDF 텍스트 추출로 장·절·조·항·목/이스케이프/페이지번호 확인, sanitizer XSS 6벡터 제거 확인 |
| C-13 | API 연동 전 선행 4건 (T-87·T-12·T-40·T-31 + T-30 부분) | 2026-08-12 | **T-87**: 절 블록을 조 번호 순으로 재배치(`blocks`) + 절 조문까지 담는 `allGroups` 신설 — txt 내보내기·검색 카운트 누락도 함께 해소. **T-12**: `/policies/import` PDF 가져오기 화면(업로드→트리 수정→커밋). 서버 `regulation-parse`를 쓰므로 글꼴 기반 계층 인식이 클라이언트 텍스트 경로보다 정확. **T-40**: compose 시크릿 `${VAR:-default}` 전환 + `secrets-guard.ts`가 운영에서 기본값 감지 시 부팅 거부(브레이크글래스 `ALLOW_DEFAULT_SECRETS`), Runbook 7-1 절차. **T-31**: CI(typecheck·test·build + PDF 렌더러 파이썬 스모크). **T-30 부분**: vitest 28건. 부수: `pdf_render.py`가 PyMuPDF 1.24.3의 `fitz`→`pymupdf` 개명에 대응 |
| C-14 | T-86 DB 실측 + 실측 중 발견한 버그 3건 | 2026-08-13 | 빈 볼륨에서 전 구간 실측. **실측으로만 잡힌 버그 3건**: ① `PATCH /regulation-parse/:id/tree`가 트리를 통째로 파괴 — 전역 ValidationPipe의 `enableImplicitConversion`이 `roots: unknown[]` 원소를 반영 타입(Array)으로 변환해 노드가 전부 `[]`가 되고, 커밋 시 조문이 `[undefined] undefined`로 생성됨 → DTO에 `@Type(() => Object)`. ② PDF 파서가 `이 규정은 …`처럼 **한 글자+공백으로 시작하는 본문을 가나다 목 마커로 오인**(구분자에 `\s` 포함) → 구두점 필수 + 가나다 14자로 제한, 조 본문이 하위로 새던 문제 해소. ③ 커밋 응답이 `{policyId, articleCount}`인데 프론트가 `{id,title}`을 기대 → 등록 후 `/policies/undefined`로 이동하던 것 수정. 부수: PDF 내보내기 201→200. 남은 한계는 T-88·T-89 |
| C-15 | 법제처 연동 백엔드 (T-53·T-54) | 2026-08-13 | OPEN API 승인에 따라 S2 보류 해제. **T-53** `apps/api/src/lawgokr/*` 프록시(`GET /lawgokr/status·search·laws/:mst`, admin·editor). 인증값은 서버 전용이며 URL에 실려 나가므로 **로그에 URL을 남기지 않는다**. 캐시는 `REDIS_URL` 있으면 Redis, 없으면 프로세스 메모리(로컬 dev 대응) — 본문 1건이 약 750KB이고 인증값 단위로 사용량이 제한되어 필수. **T-54** 조·항·호·목 → `RegulationArticleNode` 매퍼로 PDF 경로와 타입을 공유(미리보기·커밋 재사용). 규격 문서에 없어 실응답으로만 확인된 3가지를 전제로 구현: ① 오류도 **HTTP 200**(`{result,msg}` 인증실패 / `{Law:"일치하는 법령이 없습니다"}`), ② `항`·`호`·`목`이 1건이면 객체·다건이면 배열, ③ 장/절 제목 행의 `조문번호`는 장 번호가 아니라 **그 장 첫 조의 번호**(→ `조문내용` 파싱). 검증: 자동차관리법(MST=286989) 실응답으로 조 206/206·장 16/16 무손실, parentId 불일치 0, 캐시 재조회 1016ms→0ms·1371ms→3ms, 오류 4경로 확인. vitest 15건 추가 |
| C-15 | T-56 신구조문대비표 | 2026-08-13 | 법제처 연동 없이 **우리 규정끼리** 대비. `policy-compare.ts`(순수 로직) + `GET :id/compare`. 조문 짝짓기는 **`articleId` 기준** — 개정으로 조 번호가 바뀌어도 올바로 대응된다(번호 기준이면 엉뚱한 조문끼리 비교). 공백·줄바꿈 정규화로 서식 차이는 개정으로 치지 않고, 본문이 같아도 제목이 바뀌면 개정으로 본다. 2단 대비표 화면(구조문 삭제분 취소선 / 신조문 추가분 강조)·인쇄·변동없음 숨기기. 실측: 제1조에 실제 개정(2026-06-01 시행)을 만들어 낱말 diff 확인, 제정 전→현재 전부 신설, from>to·잘못된 날짜·파라미터 누락 400, 교차 테넌트 404, 미인증 401. 단위 테스트 8건 | 
| C-08 | 전문 보기 조문 중복 출력 버그 수정 | 2026-08-01 | 대표님 화면 제보. 조 루트 행이 `main`·`items` 두 경로로 렌더돼 제목·본문이 2회 출력. `fullViewGroups.ts` 신설로 React/HTML 두 렌더러가 동일 행 목록 사용. 부수 수정: 기본 템플릿 `showArticleTitle`가 조 제목에 미적용, 설정 미리보기 구조 불일치, 조 번호가 장마다 1로 리셋, 도구줄이 보유 기능(별표·서식)을 ‘추후 제공’으로 오안내 |
| C-16 | 가져오기 조 번호 재번호 버그 수정 | 2026-08-15 | 대표님 화면 재확인 중 발견. `renumber`가 **장마다 1번부터** 다시 매겨(`articleIdx + 1`) 제2장이 다시 제1조로 시작 — 목차에서 조문을 특정할 수 없다. 조 번호는 장을 가로질러 이어지는 것이 법령·사내규정 공통 표기이고 `seed.ts`도 같은 규칙(제1장 제1·2조 → 제2장 제3조)을 주석으로 명시하고 있었다. `renumberChapters()`를 `policyImportHierarchy.ts`에 순수 함수로 분리. 부수: `renumber`가 `trimEmptyArticles`보다 **먼저** 돌아 빈 조 제거 후 번호에 구멍(1,2,4)이 남던 것도 순서를 뒤집어 해소. 회귀 테스트 5건 추가(옛 동작에서 3건 실패 확인). **주의: 이미 잘못 저장된 규정은 재배포로 고쳐지지 않는다** — 해당 규정 재가져오기 또는 수동 재번호 필요 |
| C-17 | T-55 법제처 가져오기 탭 | 2026-08-17 | 법제처 법령으로 **업로드와 같은 파싱 세션**을 만들어(`POST /regulation-parse/from-lawgokr`) 미리보기·수정·커밋 단계를 코드 분기 없이 재사용. 소스가 늘어도 갈라지는 곳은 1단계뿐이다. `LawGoKrSearchPanel`(검색·페이지네이션·가져오기) + 미리보기에 출처·공포일·시행일·원문링크 표시. OC 미설정 시 검색 전에 안내 패널을 띄운다(실패를 기다리게 하지 않는다). 실측: DI·라우트 등록(`:id`보다 먼저)·503/400/401·법제처 모양 트리 커밋까지 확인. **T-88 선행 필요 확인** — 커밋이 항·호를 별도 조로 평탄화해 법제처 트리(조>항>호)가 조 5개가 됐다. 그대로는 조 번호가 전부 틀린다 |
| C-18 | T-88 커밋 시 조·항·목 계층 반영 | 2026-08-17 | 커밋이 트리를 전부 조로 평탄화하던 것을 `commit-rows.ts`(순수 함수)로 분리해 해소. **분류 기준을 depth가 아니라 `articleNumber`(norm)로 잡은 것이 핵심** — PDF 파서는 `제1조의2`(`L1-2`)를 depth 1로 제1조의 **자식**으로 넣어서, depth만 보면 항으로 오인한다. norm 어휘(`L`조·`C`항·숫자 호·`H`목·`P/CH/S/SS` 편장절관)는 PDF 빌더와 법제처 매퍼가 공유한다. 법령은 조>항>호>목 4단인데 우리 모델은 3단이라, 남는 한 단(호 아래 목)은 가짜 조문을 만들지 않고 부모 본문에 이어 붙인다. 부수: 제목에 내부 코드가 새어 나오던 것(`[L1] 목적`, `[C1] `) 제거 — 번호는 이제 구조 필드에 들어간다. 죽은 `flattenNodes` 제거. 테스트 11건(코드 앞글자 충돌 `CH1`/`C1`, `P1`/`PN1` 포함). 실측: 실제 norm 트리 커밋 → 조2 항1 목1 / 조2 항2 / 조3(제1조의2) / 조4 확인. **남은 한계: 장·절 제목 행이 아직 조로 들어가 조 번호를 밀어낸다(T-89)** |
| C-19 | T-89 가져오기 장·절 구조 반영 | 2026-08-18 | ① 장 제목이 실제 `Chapter`, 절이 `Section`이 된다(예전엔 항상 숨김 장 1개). ② **PDF 파서에 편·장·절·관 패턴이 아예 없어서** `제2장 …` 줄이 직전 조 본문 끝에 눌어붙던 것을 `matchHeader`에 추가해 해소. 헤딩 depth를 음수(편 -4 … 관 -1)로 둬 조(0)가 그 아래로 중첩된다 — 미리보기는 `Math.max(depth,0)`로 클램프해 영향 없음. ③ 조 제목 norm 노출은 C-18에서 이미 해소. 우리 모델은 장>절 2단이라 편(編) 자리가 없다 — 조문을 못 받은 장·절은 버리므로 "편>장>조"는 편이 사라지고 장이 최상위가, "편>조"는 편이 곧 장이 된다. 테스트 9건 추가(파서 4 + 계획 5). 헤딩 패턴을 빼면 3건이 실패하는 것까지 확인. 실측: 장 1개 + 조3 + 항2 + 목1 커밋 → 장이 조 번호를 밀어내지 않음(예전 2·3·4조 → 1·2·3조) |
| C-20 | T-71 규정 체계도 | 2026-08-19 | `Policy.parentId` 자기참조 추가(마이그레이션 `policy_hierarchy`, 순수 추가형). T-56 3단비교가 쓸 모델과 동일. `GET /policies/hierarchy`가 트리를 돌려주고, 규정 목록에 목록/체계도 뷰 토글 + 행마다 상위 선택. 체계는 한 건씩이 아니라 전체를 놓고 정리하므로 상세 설정이 아니라 체계도 화면에 뒀다. **순환 방어 2중**: 서버가 조상 사슬을 거슬러 확인해 400으로 거부(`assertParentAllowed`), 조립기(`buildPolicyForest`)는 이미 순환에 든 데이터도 최상위로 올려 **화면에서 사라지지 않게** 한다 — 링크만 걸면 순환 노드가 어느 루트에도 안 걸려 통째로 증발한다. 테스트 8건(방어를 빼면 규정 3건이 1건으로 줄어드는 것 확인). 실측: 3단 트리 구성, 순환 400, 자기지정 400, 상위 삭제 시 하위가 최상위로 승격 |

---

## 결정 대기 (개발 작업 아님 — 대표·PM 안건)

개발 태스크와 섞이면 안 되는, 사업/방향 결정 항목. 결정이 나면 위 백로그에 실제 작업으로 내려온다.

| ID | 안건 | 결정 시 영향 | 출처 |
|----|------|------|---|
| D-01 | 정식 서비스명 확정 (코드에 veda/astrum 잔재) | PlatformBranding 기본값·i18n 정리 작업 발생 | [기획서 1·7절](../01_프로젝트기획서/프로젝트기획서.md) |
| D-02 | 가격 정책 (Starter/Pro/Enterprise) | 결제·플랜 UI 문구 확정 | [기획서 5·7절](../01_프로젝트기획서/프로젝트기획서.md) |
| D-03 | 실 PG 연동 여부·시점 | T-17 및 결제 연동 작업 규모 결정 | [기획서 7절](../01_프로젝트기획서/프로젝트기획서.md) |
| D-04 | ~~git 도입~~ + 브랜치/커밋 전략 | **git 도입은 완료**(origin/develop 운영 중, T-31 블로커 해소). 남은 안건은 브랜치 전략·PR 규칙 합의뿐 | [개발컨벤션 5절](../05_개발컨벤션/개발컨벤션.md) |
| D-05 | 테스트·CI 도입 우선순위 | T-30·T-31 착수 시점 | [기획서 7절](../01_프로젝트기획서/프로젝트기획서.md) |

---

## 작성 가이드

- **ID 규칙**: 작업 `T-##`, 완료 `C-##`, 결정 `D-##`. 번호는 재사용하지 않음(삭제해도 결번 유지).
- 새 할 일은 해당 버킷 표에 행 추가 + 반드시 **출처 문서 링크**를 건다(문서 기준 개발 원칙 유지).
- 상태만 바뀌면 표에서 즉시 수정. 완료되면 "완료" 섹션으로 옮기고 완료일 기록.
- 이 문서는 git/이슈 트래커 도입(D-04) 전까지의 백로그다. git 도입 후에는 각 `T-##`를 이슈로 옮기고 이 표는 인덱스로 축소할 수 있다.
