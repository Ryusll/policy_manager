# 화면설계서

작성일: 2026-07-21
근거: `apps/web/src/App.tsx`(라우팅), `apps/web/src/pages/*`, `docs/policy_manager_개발정의서.docx`, `docs/VEDA_FeatureList.docx`

## 1. 라우트 맵 (`App.tsx`)

| 경로 | 페이지 | 접근 제한 |
|---|---|---|
| /login | LoginPage | 비로그인 전용 |
| /register | RegisterPage | 비로그인 전용 |
| /oauth/callback | OAuthCallbackPage | 비로그인 전용 (OAuth 리다이렉트 수신) |
| / | DashboardPage | 로그인 필요 |
| /policies | PoliciesPage | 로그인 필요, viewer 접근 불가 |
| /policies/:id | PolicyDetailPage | 로그인 필요, viewer 접근 가능 |
| /search | SearchPage | 로그인 필요, viewer 접근 가능 |
| /variables | VariablesPage | 로그인 필요, viewer 접근 불가 |
| /settings | SettingsPage | 로그인 필요, viewer 접근 불가 |
| /integrations | IntegrationsPage | 로그인 필요, viewer 접근 불가 |
| /admin, /platform-admin | PlatformAdminPage | `global_admin`만 |
| /about | AboutPage | 로그인 필요, viewer 접근 가능 |

`PrivateRoute`(`App.tsx`) 규칙: 미인증 시 `/login`으로 리다이렉트. `viewer` 역할은 `/search`, `/policies/:id`, `/about`만 허용되고 나머지는 `/search`로 강제 이동. `/admin`·`/platform-admin`은 `platformRole === 'global_admin'`이 아니면 `/`로 이동.

## 1.5 와이어프레임 (개략)

코드 기준으로 재구성한 개략 레이아웃이다. 픽셀 정밀 시안이 아니라 영역·구성 파악용이며, 실제 반응형 동작은 코드가 기준이다.

### 공통 셸 (Layout.tsx)
```
┌──────────────────────────────────────────────────────────────┐
│ [로고+규정관리 시스템]   대시보드 규정 검색 변수 설정 …   🔔  [👤이름 ▾] │  ← 헤더 nav + NotificationBell + UserMenu
├──────────────────────────────────────────────────────────────┤
│ 홈 > 현재 위치            (breadcrumb, 규정상세·통합관리는 페이지 자체 사용) │
│                                                              │
│                   〈페이지 본문: <Outlet/>〉                    │
│                                                              │
├──────────────────────────────────────────────────────────────┤
│           Powered by [운영사 로고] 법인명 · 사업자번호           │  ← AppFooterBranding (플랫폼 브랜딩)
└──────────────────────────────────────────────────────────────┘
  ※ 우상단 토스트(ToastHost), 최초 방문 시 OnboardingTour 오버레이
```

### 대시보드 (DashboardPage)
```
┌ PageHeader: "대시보드" ─────────────── [통합검색 바로가기] ┐
├──────────────────────────────────────────────────────────┤
│ ┌ 전체 ┐ ┌ 시행중 ┐ ┌ 비활성 ┐ ┌ 내 권한 ┐   ← KPI stat-card ×4 │
│ │  12  │ │   8    │ │   4    │ │ admin  │                     │
│ └──────┘ └────────┘ └────────┘ └────────┘                     │
│ 최근 수정 (6건)                                               │
│  • 정보보호 관리규정   [보안]  시행중   2026-07-19            │
│  • 취업규칙            [인사]  비활성   2026-07-15            │
│  … (없으면 EmptyState + [규정 등록] CTA)                       │
└──────────────────────────────────────────────────────────┘
```

### 규정 상세 (PolicyDetailPage) — 핵심 화면, 분절 보기
```
┌ 규정 목록 > 정보보호 관리규정 · [전문 보기│분절 보기] ── [전체화면][인쇄][.txt][주소복사] ┐
├───────────┬──────────────────────────────────────────────────────────┤
│ 좌측 사이드바 │ 상단: [제정·개정이유][3단비교][신구법비교]… (disabled, 8종)   │
│ ┌ 탭 ────┐ │ ┌ 조문 본문 (col-span-9) ──────┬ 우측 비교패널(col-span-3) ┐ │
│ │본문·목차│ │ │ 제6조 (주주의 대리인의 출석) │ 판·법·규 배지            │ │
│ │ 연혁   │ │ │  ① 항 본문 …                │ 관련 링크 미리보기       │ │
│ │부칙·별표│ │ │  ② 항 본문 …                │ [연관 고정/네비]         │ │
│ └────────┘ │ │  ┌ 조·항·목 구조 편집 패널 ┐ │ 개정일·시행일 편집       │ │
│ [목차 필터] │ │  │ 조/항/목 번호·제목 [저장]│ │ 개정 알림 수신자 설정    │ │
│ 장>조>①>1. │ │  └───────────────────────┘ │                         │ │
│ [+항][+조] │ │ 버전: 초안→검토→시행 [승인]  │ 코멘트 [작성][해결]      │ │
└───────────┴──┴─────────────────────────────┴─────────────────────────┘
  ※ 전문 보기: 우측 패널 없이 본문 전폭, 규정 전체를 인쇄 레이아웃으로 렌더
```

### 설정 (SettingsPage)
```
┌ PageHeader: "설정" ──────────────────────────────────┐
├──────────┬───────────────────────────────────────────┤
│ [UI]     │ 〈선택 탭 내용〉                            │
│ [팀·알림] │  UI:   테마 프리셋(forest/ocean/slate/wine) │
│ [양식관리]│        + 로고·브랜드마크 + 헤더/로그인 미리보기 │
│          │  팀:   팀원 초대 · 알림 수신 팀 CRUD          │
│          │  양식: 템플릿 편집·미리보기(Pro+) · 변경이력   │
└──────────┴───────────────────────────────────────────┘
  ※ Starter는 브랜딩·양식 편집 비활성 → 업그레이드 유도
```

## 2. 로그인 / 회원가입 (LoginPage / RegisterPage / OAuthCallbackPage)

- 이메일+비밀번호 로그인, 조직 slug 입력(회원가입 시 신규 테넌트 생성 또는 기존 테넌트 참여)
- Google OAuth 로그인 버튼 (`GET /auth/google` 리다이렉트 → `OAuthCallbackPage`에서 토큰 수신)
- 헤더: 테넌트 브랜딩(로고 있으면 이미지, 없으면 BrandBadge)

## 3. 대시보드 (DashboardPage)

- PageHeader + 통합검색 바로가기
- KPI stat-card 4개: 전체 규정 수 / 시행중 / 비활성 / 권한(역할 표시)
- 최근 수정 규정 6건 리스트 — 부서·분류 배지, 시행 상태 배지
- 데이터 없을 때 EmptyState + 규정 등록 CTA

## 4. 규정 목록 (PoliciesPage)

- PageHeader + 규정 등록 버튼
- 등록 탭: 기본 등록 / 기존 규정 가져오기(Import Wizard)
- 담당 부서·규정 분류 메타데이터 입력 필드
- 검색 + 부서/분류 필터, 목록은 `gov-table` 스타일
- 등록 성공 시 토스트 알림
- 하단: 가져오기 이력(PolicyImportLog) 목록

### 4.1 규정 가져오기(Import Wizard)
- 파싱 프로필: 혼합형 / 한국형 / 영문형
- 입력 방식: 파일 업로드(TXT/DOCX/PDF) 또는 텍스트 붙여넣기
- 실시간 미리보기: 인식된 장 수 / 조문 수
- 고급 옵션(접이식): 구분 모드(auto/blank_block/line_each/delimiter/markdown), 원문 정제(공백·머릿글 제거, 따옴표 통일, `**굵게**` 래핑)
- 등록 시 번호 자동 재정렬, 빈 조문 자동 제거
- 「항·목 자동 인식」(기본 ON): 원문의 ①②… → 항, `1. 2. …` → 목으로 인식해 계층까지 등록. 등록 전 인식 개수를 안내하며, 패턴이 없으면 조 단위로만 등록 (2026-07-30, T-51)
- 원문에 장 표기가 없으면 **임의의 "제1장 총칙"을 만들지 않고** 숨김 장에 담는다 (2026-08-02, SRS POLICY-6)

## 5. 규정 상세 (PolicyDetailPage) — 핵심 화면

### 5.1 공통
- 로딩: LoadingBlock / 규정 없음: EmptyState
- Breadcrumb(페이지 자체 구현, 전역 breadcrumb 숨김): 규정 목록 > {규정명} · {전문 보기|분절 보기}

### 5.2 좌측 사이드바 3탭
| 탭 | 기능 |
|---|---|
| 본문·목차 | 목차(TOC) 트리 + 조문 검색 필터 |
| 연혁 | 조문별 현재 게시(published) 버전 스냅샷 |
| 부칙·별표 | PolicyAppendix CRUD (부칙/별표/서식) |

### 5.3 목차(TOC) 패널
- 보이기/숨기기, 고정 모드(sessionStorage 저장)
- 조문번호·제목 필터 검색
- 계층 표시: 장 > **절(선택)** > 조(제N조) > 항(①②…) > 목(1. 2. …)
- 절은 선택 계층이라 한 장 안에 "절에 속하지 않는 조"와 "절에 속한 조"가 공존한다. 절 미소속 조를 먼저 보이고, 이어서 `제N절` 헤더 + 소속 조문을 번호순으로 표시
- 장 블록마다 「+ 절 추가」 버튼, 절 헤더에 「삭제」(조문은 보존되고 절 연결만 해제), 절별 「제N절에 조 추가」
- 조 그룹별 「+ 제N조 항 추가」, 「+ 조 추가」 버튼 — 항 추가 시 다음 항 번호 자동 제안
- **장을 만들지 않고 조부터 추가 가능**: 장 없이 「조 추가」를 누르면 숨김 장이 자동 생성되어 조가 담기고, 화면·인쇄에는 장 제목이 보이지 않는다
- `suppressHeader` 장은 장 제목 숨기고 조문만 표시

### 5.4 본문 영역
- **전문 보기**: 규정 전체를 한 화면에 렌더 (인쇄용 레이아웃과 동일 계열)
- **분절 보기**: 조문 단위 선택·편집, 우측에 판·법·규 배지 및 연관 항목 미리보기 패널
- 버전 워크플로 UI: 초안 작성 → 검토 요청 → 시행 승인(승인 사유 필수, 기존 published는 자동 archived) · 반려·폐지도 화면에서 처리(T-10)
- 신구 버전 비교(diff): 줄 단위 / 단어 단위
- 규정 개정일·시행일 편집 + 개정 알림 수신자 설정(팀/개인)
- 전체화면 보기, 인쇄(window.print, 선택 템플릿 렌더), 전문 `.txt` 다운로드, 주소 복사
- 조문 코멘트: 작성 + 처리완료/미처리 토글
- **첨부파일 모달**: 목록(파일명·크기) + 다운로드 링크. admin·editor 는 「파일 추가」와 행별 「삭제」(확인 대화상자에서 파일명을 보여 준다). viewer 는 목록·다운로드만.
  - 목록에 뜨는 이름은 **올린 그대로의 이름**이다. 디스크 저장명은 따로 있고(경로 탈출 방어 때문에 ASCII 로 제한), 원래 이름은 그 안에 실어 되돌린다(T-90)
  - 업로드·삭제는 감사 로그에 남는다(`policy.file.upload` / `policy.file.delete`)
- 외부 링크: 표준국어대사전, Google 법령 검색
- 조·항·목 구조 편집 패널: 조 번호·항·목 번호·제목 수정 → `PUT /policies/:id/chapters/:chapterId/articles/:articleId`
- 조문 추가 모달: 판·법·규 체크 + 근거 메모, 초기 본문 입력 시 draft v1 버전 자동 생성

## 6. 통합검색 (SearchPage)

- PageHeader, LoadingBlock, EmptyState 공통 패턴 적용
- 검색 대상: 규정명·코드 + 게시된(published) 조문 본문/제목
- 검색 엔진: PostgreSQL `pg_bigm` 우선, 실패 시 `ILIKE` 폴백
- 결과 하이라이트·스니펫 + 규정 상세 딥링크
- 판·법·규 연관 항목 미리보기 및 고정(pin)

## 7. 변수 (VariablesPage)

- 템플릿용 변수(`{{VAR}}`) CRUD
- 변수 사용 이력(VariableUsage) 조회 — 어떤 버전에서 사용 중인지 추적

## 8. 설정 (SettingsPage)

좌측 탭 구조:

| 탭 | 기능 |
|---|---|
| UI | 테마 프리셋(forest/ocean/slate/wine) + 커스텀 Primary/Accent 색상, 브랜드 마크(1~2자)·로고 업로드·크기, 헤더/로그인 미리보기 (Pro+ 플랜 전용) |
| Invite(팀) | 팀원 초대(admin이 이메일·역할로 생성, Pro+), 개정 알림 수신 팀(NotificationGroup) CRUD |
| 양식 관리 | PolicyTemplate 편집·미리보기 — Starter는 비활성, Pro는 basic 빌더, Enterprise는 HTML/CSS 자유 편집 |

- 플랜 비교 모달(Starter/Pro/Enterprise) 노출

## 9. 연동 (IntegrationsPage)

- Enterprise 플랜 API 연동 안내 (`GET /integrations/api-access`)

## 10. 통합 관리자 (PlatformAdminPage) — `global_admin` 전용

- 테넌트 목록·통계, 테넌트별 사용자 목록
- 테넌트 플랜 변경(starter/pro/enterprise), 테넌트 내 사용자 역할 변경
- 사용자 플랫폼 역할(global_admin) 부여/해제
- 플랫폼 브랜딩(법인명·사업자번호·제품명·로고) 편집
- 라이선스(플랜) 만료 임박 테넌트 목록

## 11. 소개 (AboutPage)

- 서비스 소개 정적 페이지 (viewer 역할도 접근 가능)

## 12. 공통 UI 인프라 (전 페이지 공통)

| 컴포넌트 | 파일 | 설명 |
|---|---|---|
| Layout | components/Layout.tsx | 헤더 nav, breadcrumb, UserMenu, NotificationBell |
| PageHeader | components/ui/PageHeader.tsx | 페이지 제목·설명·액션 영역 |
| EmptyState | components/ui/EmptyState.tsx | 데이터 없음 안내(compact 옵션) |
| LoadingBlock | components/ui/LoadingBlock.tsx | 중앙 스피너 + 라벨 |
| ToastHost / toastStore | components/ui/ToastHost.tsx, stores/toastStore.ts | 우상단 토스트(success/error/info), 4.2초 자동 닫힘 |
| UserMenu | components/UserMenu.tsx | 아바타·이름·드롭다운(프로필/언어/플랜/투어/로그아웃) |
| NotificationBell | components/NotificationBell.tsx | 미읽음 수, 알림 목록, 규정 바로가기 |
| OnboardingTour | 최초 방문 5단계 화면 안내 |
| AppBrandLockup / AppFooterBranding / BrandBadge | components/*.tsx | 테넌트 브랜딩(헤더) / 플랫폼 브랜딩(푸터) / 로고 없을 때 텍스트 배지 |

## 13. 미구현 · 플레이스홀더 (화면에 노출되나 비활성)

FeatureList 14절 기준: 제정·개정이유, 3단비교, 신구법비교, 법령체계표, 법령비교, 음성·점자 지원 등 8개 버튼은 UI상 disabled 상태(추후 제공). 규정 `isActive` 토글은 표시만 되고 편집 UI 없음.
