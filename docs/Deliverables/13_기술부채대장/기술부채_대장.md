# 기술부채 대장

작성일: 2026-07-20 (최종 갱신: 2026-07-21, 스모크 테스트 결과 반영)
작성 기준: 프로젝트 실사(코드베이스 점검) 결과 시딩. 발견 시점마다 항목 추가.

| # | 항목 | 설명 | 발견일 | 심각도 | 상태 |
|---|------|------|--------|--------|------|
| 1 | 자동화 테스트 없음 | `apps/api`, `apps/web` 전체에 `*.spec.ts`/`*.test.ts` 파일 없음. 회귀 검증이 수동에 의존. **2026-08-22 해결**: 단위 181건 + DB 통합 130건. 플랜×권한 조합은 [점검표](../12_테스트결과서/플랜권한_점검표.md)와 짝을 이루는 매트릭스 테스트 84건(T-59) | 2026-07-20 | 높음 | 해결(2026-08-22) |
| 2 | CI 파이프라인 없음 | `.github/workflows` 등 CI 설정 없음. 빌드/테스트가 로컬 실행에만 의존. **2026-08-12 해결**(T-31), 통합 테스트 job 추가(T-30) | 2026-07-20 | 중간 | 해결(2026-08-21) |
| 3 | 루트 README 없음 | 저장소 최상위에 프로젝트 소개·실행 방법을 안내하는 `README.md` 없음 → 2026-07-21 신규 작성 | 2026-07-20 | 중간 | 해결(2026-07-21) |
| 4 | docker-compose 기본 시크릿 하드코딩 | `JWT_SECRET`, `POSTGRES_PASSWORD`, `MINIO_ROOT_PASSWORD` 등이 예시값으로 커밋되어 있음. **2026-08-12 해결**(T-40): `${VAR:-default}` 전환 + 시크릿 가드가 운영 기동 시 기본값을 감지하면 부팅 거부. 상세는 [15_보안검토서](../15_보안검토서/보안_검토서.md) | 2026-07-20 | 높음 | 해결(2026-08-12) |
| 5 | Prisma 스키마 블록 주석으로 빌드 실패 | `schema.prisma`의 `/** */` 블록 주석 3곳(166·321·347행)은 Prisma가 지원하지 않는 문법 → `prisma generate` 실패로 **API 이미지 빌드 불가**. 스모크 테스트 중 발견, `///` 로 수정 완료 | 2026-07-21 | 높음 | 해결(2026-07-21) |
| 6 | 비결정적 Docker 빌드 | 락파일이 리포 루트에만 있는데 api 빌드 컨텍스트가 `./apps/api`라 `npm install`이 매번 새로 해석됨(#5를 표면화한 원인). **2026-08-02 해결**: 빌드 컨텍스트를 리포 루트로 올리고 `npm ci --workspace=apps/api`로 전환 | 2026-07-21 | 높음 | 해결(2026-08-02) |
| 7 | Docker 빌드: pymupdf 설치 실패 | **원인 확정**: PyMuPDF는 musllinux(Alpine) wheel을 제공하지 않아 소스 빌드로 넘어가다 실패(`cc` 127). **2026-08-02 해결**: 베이스를 `node:20-slim`(Debian)으로 전환하고 `--only-binary=:all:`로 소스 빌드를 차단. Prisma `binaryTargets`도 `debian-openssl-3.0.x`로 변경 | 2026-07-21 | 높음 | 해결(2026-08-02) |
| 8 | baseline 마이그레이션 부재 | 첫 마이그레이션이 스키마를 만들지 않아 `migrate deploy`가 실패하고 `db push --accept-data-loss` 폴백에 의존했음(데이터 손실 위험). **2026-08-02 해결**: 13개 마이그레이션을 `00000000000000_init`(전체 스키마) + `00000000000001_enable_pgbigm`으로 스쿼시. 빈 DB에서 `migrate deploy`만으로 재현 확인(드리프트 0). 폴백은 제거하고 baseline 자동 처리로 대체 | 2026-07-21 | 높음 | 해결(2026-08-02) |

> 위 5~8은 2026-07-21 스모크 테스트에서 발견됐고 **2026-08-02 전부 해소**됐다. 빈 볼륨에서 `docker compose up -d --build` → 마이그레이션만으로 기동 → 로그인·API 동작까지 실측 확인.

## 작성 가이드
- 새 항목 발견 시 표에 행 추가, 발견일은 실제 날짜로 기록
- 해결되면 상태를 "해결"로 변경하고 해결일·PR 링크 등을 비고에 남길 것 (표 형식이 부족하면 항목별 섹션으로 확장)
