# 기술부채 대장

작성일: 2026-07-20 (최종 갱신: 2026-07-21, 스모크 테스트 결과 반영)
작성 기준: 프로젝트 실사(코드베이스 점검) 결과 시딩. 발견 시점마다 항목 추가.

| # | 항목 | 설명 | 발견일 | 심각도 | 상태 |
|---|------|------|--------|--------|------|
| 1 | 자동화 테스트 없음 | `apps/api`, `apps/web` 전체에 `*.spec.ts`/`*.test.ts` 파일 없음. 회귀 검증이 수동에 의존 | 2026-07-20 | 높음 | 미해결 |
| 2 | CI 파이프라인 없음 | `.github/workflows` 등 CI 설정 없음. 빌드/테스트가 로컬 실행에만 의존 | 2026-07-20 | 중간 | 미해결 |
| 3 | 루트 README 없음 | 저장소 최상위에 프로젝트 소개·실행 방법을 안내하는 `README.md` 없음 → 2026-07-21 신규 작성 | 2026-07-20 | 중간 | 해결(2026-07-21) |
| 4 | docker-compose 기본 시크릿 하드코딩 | `JWT_SECRET`, `POSTGRES_PASSWORD`, `MINIO_ROOT_PASSWORD` 등이 예시값으로 커밋되어 있음. 상세는 [15_보안검토서](../15_보안검토서/보안_검토서.md) 참고 | 2026-07-20 | 높음 | 미해결 |
| 5 | Prisma 스키마 블록 주석으로 빌드 실패 | `schema.prisma`의 `/** */` 블록 주석 3곳(166·321·347행)은 Prisma가 지원하지 않는 문법 → `prisma generate` 실패로 **API 이미지 빌드 불가**. 스모크 테스트 중 발견, `///` 로 수정 완료 | 2026-07-21 | 높음 | 해결(2026-07-21) |
| 6 | apps/api 락파일 없음 (비결정적 빌드) | `apps/api/package-lock.json` 부재 → 클린 설치 때마다 `prisma ^5.9.1`이 최신(5.22.0)으로 해석됨. 5.22.0이 위 #5를 표면화. 빌드 재현성 없음 | 2026-07-21 | 높음 | 미해결 |
| 7 | Docker 빌드: pymupdf/pdfplumber 설치 실패 | 런너 스테이지 `pip3 install pymupdf pdfplumber`가 Alpine musl에서 C 툴체인/호환 wheel 부재로 실패(`cc` 127) → **이미지 빌드 불가**. 스모크 테스트에서는 해당 라인을 임시 제외하고 진행. 근본 해결 필요(빌드툴 추가 / debian-slim 베이스 / 호환 wheel 고정 중 택1) | 2026-07-21 | 높음 | 미해결 |
| 8 | baseline 마이그레이션 부재 | 첫 마이그레이션이 스키마를 생성하지 않아(`enable_pgbigm`부터 시작) `prisma migrate deploy`가 "relation \"users\" does not exist"로 실패. 현재는 `db push --accept-data-loss` 폴백에만 의존(운영 데이터 손실 위험). 스모크 테스트에서 실제 재현됨. [10_ADR ADR-0005](../10_ADR_의사결정기록/ADR.md) | 2026-07-21 | 높음 | 미해결 |

> 위 5~8은 2026-07-21 `docker compose up` 스모크 테스트(2026-07-21) 중 실측으로 확인. #5·#6·#7·#8이 겹쳐 **클린 체크아웃에서 문서상 배포 절차가 그대로 동작하지 않음** — 우선 해소 대상.

## 작성 가이드
- 새 항목 발견 시 표에 행 추가, 발견일은 실제 날짜로 기록
- 해결되면 상태를 "해결"로 변경하고 해결일·PR 링크 등을 비고에 남길 것 (표 형식이 부족하면 항목별 섹션으로 확장)
